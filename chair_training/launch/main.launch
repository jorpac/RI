<launch>

    <!-- Load the parameters for the algorithm -->
    <rosparam command="load" file="$(find chair_training)/config/qlearn_params.yaml" />

    <!-- Launch the training system -->
    <node pkg="chair_training" name="chair_gym" type="start_training.py" output="screen"/>
</launch>